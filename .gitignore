node_modules

# Output
.output
.vercel
.vercel-*
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Playwright
/test-results
/playwright-report
/blob-report
/playwright/.cache

# Vitest coverage
/coverage

# Bundle analysis (generated on demand by `pnpm run analyze`)
/bundle-report.html

# CLI bundle (generated by `pnpm run cli:build`; published to npm but
# rebuilt deterministically from source, so not committed)
/cli/dist

# TypeDoc API reference (generated on demand by `pnpm run docs:api`;
# regenerable from JSDoc + types, so not committed)
/docs/api

# Claude Code runtime
.claude/scheduled_tasks.lock
